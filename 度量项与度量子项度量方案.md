# 度量项与度量子项度量方案
 
## 一、度量分类总览

| 度量分类 | 目的 | 度量项 | 度量项权重 |
|---|---|---|---| 
| 系统规格 | 度量在系统规格层面（带宽利用、搬运切分、对齐约束）对用户的暴露程度 | 访存带宽 | 1 |
| 编译友好 | 度量编译时能否自动消化硬件细节，减少用户手动调优的代价 | api 编译友好、编译效率 | 暂不度量 |
| 内存模型 | 度量在内存层级、离散访问、cache一致性等方面对用户的认知与编码代价 | buffer 层级、cache | 5/9、4/9 |
| 计算模型 | 度量在计算指令原子性、完备性、参数复杂度等方面对用户的编程负担 | 计算指令、随路计算 | 2/3、1/3 |
| 控制模型 | 度量在多流水编排、同步原语、生产者消费者模型等方面的控制复杂度 | Scalar、同步、多pipeline 并行、头开销、生产者消费者模型 | 1/6、1/3、1/6、1/6、1/6 |
| 访存模型 | 度量在数据搬运地址计算、格式转换、tiling参数等方面的访存编码代价 | 申请与管理、访存指令 | 1/3、2/3 |
| 调试调优 | 度量在错误异常处理、芯片断点调试、可观测性、性能度量等方面的调试调优能力 | 错误异常处理、芯片调试断点、可观测维度、性能度量 | 1/4、1/4、1/16、7/16 |
| Agent 友好 | 度量系统对 AI Agent / Copilot 自动生成算子代码的友好程度，包括代码可机器理解性、接口规范性、零手动修复率 | Agent性能达成率 | 1 |
| 开发效率 | 度量开发多版本算子时的代码修改代价与性能提升比，衡量版本迭代的开发投入产出效率 | 版本开发代价 | 1 |

---

## 二、度量项与度量子项


### 2.1 系统规格

> 度量在系统规格层面（带宽利用、搬运切分、对齐约束）对用户的暴露程度

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---| 
| 1.a.1 | 访存带宽 | 访存带宽 | 基于TileSize/BurstLen/Stride/CoreNum等维度展开度量，确认相同场景下达成带宽利用率约束，约束越少分数越高 | 1 |

### 2.2 编译友好（暂不度量）

> 度量编译时能否自动消化硬件细节，减少用户手动调优的代价

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 2.a.1 | api 编译友好 | api 编译友好 | 编译效率 | (暂不度量) |
| 2.b.1 | 编译效率 | 编译效率 | 度量编译path的复杂度 | (暂不度量) |


### 2.3 内存模型

> 度量在内存层级、离散访问、cache一致性等方面对用户的认知与编码代价

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 3.a.1 | buffer 层级 | 对齐颗粒度 | 内存对齐符合C语言约束 | 1/9 |
| 3.a.2 | buffer 层级 | bank/bankgroup | 减少因 bank 冲突引入的软件代价 | 1/9 |
| 3.a.3 | buffer 层级 | 层级数 | 软件感知层级是越少越好 | 1/9 |
| 3.a.4 | buffer 层级 | 容量 | 编程因为容量而进行多种算法设计 | 1/9 |
| 3.a.5 | buffer 层级 | 层级间通路 | 内存层间可互访 | 1/9 |
| 3.b.1 | cache | dcache 一致性 | 不感知dcache 一致性; | 1/9 |
| 3.b.2 | cache | hint 行为控制 | 尽量少的hint 行为 | 1/9 |
| 3.b.3 | cache | 亲Die部署 | 尽量不感知行为 | 1/9 |
| 3.b.4 | cache | 容量 | 尽量不感知行为 | 1/9 |

### 2.4 计算模型

> 度量在计算指令原子性、完备性、参数复杂度等方面对用户的编程负担

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 4.a.1 | 计算指令 | 计算指令原子性 | 计算指令原子化;无状态残留;无上下文依赖 | 1/6 |
| 4.a.2 | 计算指令 | 计算指令性能一致性 | 相同功能下指令选择单一、分支选择最少 | 1/6 |
| 4.a.3 | 计算指令 | 指令参数 | 指令参数个数少、语义清晰、必填项少 | 1/6 |
| 4.a.4 | 计算指令 | 计算指令的完备性 | 业务表达与业界竞品对齐、覆盖关键场景 | 1/6 |
| 4.b.1 | 随路计算 | 随路计指令性能一致性 | 随路计算功能单一、分支最少 | 1/6 |
| 4.b.2 | 低bit量化 | 低bit量化 | 度量低bit支持的数据类型、精度、layout、MFU等易用性度量 | 1/6 |

### 2.5 控制模型

> 度量在多流水编排、同步原语、生产者消费者模型等方面的控制复杂度

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 5.a.1 | 多pipeline 并行 | 多pipeline 并行 | 尽量单逻辑单线程编程 | 1/6 |
| 5.b.1 | 同步 | 核间同步 | 简化同步 | 1/6 |
| 5.b.2 | 同步 | 核内同步 | 简化同步 | 1/6 |
| 5.c.1 | 生产者消费者模型 | 生产者消费者模型 | 模型表达简单 | 1/6 |
| 5.d.1 | Scalar | Scalar bound | 减少Scalar Bound | 1/6 |
| 5.e.1 | 头开销 | 头开销 | 头开销度量 | 1/6 |

### 2.6 访存模型

> 度量在数据搬运地址计算、格式转换、tiling参数等方面的访存编码代价

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 6.a.1 | 访存指令 | 分型 | 简化访存指令 | 1/3 |
| 6.a.2 | 访存指令 | 非结构化访存 | — | 1/3 |
| 6.b.1 | 申请与管理 | 申请与管理 | 简化管理 | 1/3 |

### 2.7 调试调优

> 度量在错误异常处理、芯片断点调试、可观测性、性能度量等方面的调试调优能力

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 7.a.1 | 错误异常处理 | 异常类型覆盖率 | 衡量异常模型中定义的硬件异常是否具有对应的检测与处理机制 | 1/16 |
| 7.a.2 | 错误异常处理 | 异常检出率 | 故障注入后正确检测的比例，含配套指标：漏报率/误报率/重复上报率/SDC漏检率 | 1/16 |
| 7.a.3 | 错误异常处理 | 异常上报完整率与准确率 | 上报信息的必选字段完整率及字段值与真实状态的一致性 | 1/16 |
| 7.a.4 | 错误异常处理 | 首因识别率 | 级联异常中正确识别首个硬件根因的比例 | 1/16 |
| 7.b.1 | 芯片调试断点 | 断点类型覆盖率 | 已支持断点类型数/目标场景要求的断点类型总数（指令地址/源码行/函数/条件/数据/异常/Kernel/同步/指定核/全局/命中次数/超时断点） | 1/16 |
| 7.b.2 | 芯片调试断点 | 可设位置覆盖率 | 可正常设置断点的有效指令位置数/理论可调试位置总数 | 1/16 |
| 7.b.3 | 芯片调试断点 | PC定位准确率 | 实际暂停PC与期望PC一致的次数/断点命中总次数 | 1/16 |
| 7.b.4 | 芯片调试断点 | 多核暂停一致率 | 所有目标Core在规定时间内进入稳定暂停的次数/暂停请求总次数 | 1/16 |
| 7.c.1 | 可观测维度 | 观测对象覆盖率 | 支持观测的硬件与软件对象数/调试调优要求观测的对象总数（计算/搬运/存储/同步） | 1/16 |
| 7.d.1 | 性能度量 | 事件类型覆盖率 | 可采集事件类型数/性能模型要求的事件类型总数（指令/流水/Cache/内存/DMA/Barrier/Kernel等） | 1/16 |
| 7.d.2 | 性能度量 | 指标准确性 | 各维度（指令计数/执行周期/Cache命中/内存流量/流水利用率/Stall周期）的相对误差 | 1/16 |
| 7.d.3 | 性能度量 | 数据丢失率 | 因缓冲区溢出/带宽不足丢失的记录数/理论应生成记录总数 | 1/16 |
| 7.d.4 | 性能度量 | 瓶颈类型覆盖率 | 可归因的瓶颈类型覆盖率（计算/内存带宽/时延/Cache/搬运/同步/调度/并行度/指令依赖） | 1/16 |
| 7.d.5 | 性能度量 | Stall原因解释覆盖率 | 可归因到明确原因的阻塞周期/总阻塞周期（避免归入Other/Unknown） | 1/16 |
| 7.d.6 | 性能度量 | 低扰动与采集开销 | 开启观测的时间开销和行为扰动率 | 1/16 |
| 7.d.7 | 性能度量 | 端到端任务关联率 | 从软件对象关联到硬件事件的记录比例 | 1/16 |

### 2.8 Agent 友好

> 度量系统对 AI Agent / Copilot 自动生成算子代码的友好程度，包括代码可机器理解性、接口规范性、零手动修复率

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 8.a.1 | Agent性能达成率 | Agent性能达成率 | AI 自动生成的算子代码在目标硬件上的实测性能占该算子理论性能的比值 | 1 |

### 2.9 开发效率

> 度量开发多版本算子时的代码修改代价与性能提升比，衡量版本迭代的开发投入产出效率

| 编码 | 度量项(B) | 度量子项(C) | 度量目标(D) | 子项权重 |
|---|---|---|---|---|
| 9.a.1 | 版本开发代价 | 版本开发代价 | 同功能开发N个版本，度量版本迭代的代码修改代价与性能提升比例 | 1 |

---

## 三、度量子项度量计分

> 分值越高 = 易用性越好（S ∈ [0,10]，10 为最易用）。

### 3.0 度量约定（适用于全部子项）

所有度量子项只采用**可客观采集**的参数，均来自：源代码静态/动态分析、API 签名解析、友商同类指标对比、硬件规格/HW 可观测信号、以及运行期嵌入探针。统一记法如下：

- **成本型指标** $X$（值越小越易用）：代码行数、需配置参数数、用户感知约束点数、分支/种类数、时延/开销等
  $$S_X = 10 \cdot \mathrm{clamp}\!\left(1-\frac{X}{X_{ref}},\,0,\,1\right)$$
  $X_{ref}$ 为该指标的可接受参考上限（见各子项定义）。
- **比例型指标** $p \in [0,1]$（值越大越易用）：命中率、覆盖率、成功率、一致率等
  $$S_p = 10 \cdot p$$
- **友商对比** $R = X_{self}/X_{baseline}$（$X$ 越小越好时，$R\le1$ 为优；$p$ 越大越好时 $R\ge1$ 为优）
  - $X$ 越小越好：$S_{comp} = 10 \cdot \mathrm{clamp}\!\left(1/R,\,0,\,1\right)$
  - $p$ 越大越好：$S_{comp} = 10 \cdot \mathrm{clamp}\!\left(R,\,0,\,1\right)$
- **多分量聚合**（各分量已归一化到 $[0,10]$）：$S = \sum_i w_i\,S_i,\ \ \sum_i w_i = 1$


### 3.0.1 方案设计维度与数据来源

设计每个度量子项方案时，从以下方面中**选取适用者**：

| 维度 | 可参考的探查对象 | 探测方式 |
|---|---|---|
| 源代码 | 因对齐/冲突/格式转换/同步/分块等而额外写的非业务代码行数、分支数、地址计算行数 | 源码 AST/行级静态分析、运行探针 | 
| API | 完成该功能所需的 API 数、参数总数、必填参数数、调用点计数、默认值比例 | 头文件签名解析 | 
| 友商对标 | 与业界竞品等价功能的指标比值 R（代码行数、API 数、种类数、速率、开销等） | 竞品公开 SDK 等价用例对照 | 
| 硬件锚点 | 硬件约束种类数、规格特征数、原语/流水/层级数、计数器命中率 | 芯片规格书解析 | 

> 各子项中按统一公式归一化到 0~10 打分。某一维度若对该子项不适用或无法客观采集，则不在方案列出。

---

### 3.1 系统规格

> 度量在系统规格层面（带宽利用、搬运切分、对齐约束）对用户的暴露程度

#### 访存带宽

**1.a.1 访存带宽**

> 度量目标：基于大模型典型Shape场景下，发挥带宽能力的难易度；越容易发挥带宽能力易用性越高
>
> 各维度在 $p=\frac{B_{real}}{B_{peak}}$ 达到 60%/90%/99% 三档时独立度量：

| 度量维度 | 可采集参数 | 60% 带宽利用率 | 90% 带宽利用率 | 99% 带宽利用率 |
| :-- | :-- | :-- | :-- | :-- |
| TileSize | $T$＝实测达成带宽比例所需最小 TileSize（越小越好，8K~64K Byte） | $S_T^{(60)}=10\!\cdot\!\frac{\ln(65536)-\ln(T_{60})}{\ln(65536)-\ln(8192)}$ | $S_T^{(90)}=10\!\cdot\!\frac{\ln(65536)-\ln(T_{90})}{\ln(65536)-\ln(8192)}$ | $S_T^{(99)}=10\!\cdot\!\frac{\ln(65536)-\ln(T_{99})}{\ln(65536)-\ln(8192)}$ |
| BurstLen | $B$＝实测达标的 burstLen（32B~256B） | $S_B^{(60)}=10\!\cdot\!\frac{\ln(256)-\ln(B_{60})}{\ln(256)-\ln(32)}$ | $S_B^{(90)}=10\!\cdot\!\frac{\ln(256)-\ln(B_{90})}{\ln(256)-\ln(32)}$ | $S_B^{(99)}=10\!\cdot\!\frac{\ln(256)-\ln(B_{99})}{\ln(256)-\ln(32)}$ |
| stride | $s$＝实测达标的 stride 余量（s mod 64, ≠0，CL=64B），有效范围 1~63 | $S_S^{(60)}=10\!\cdot\!\frac{\ln(64)-\ln(s_{60})}{\ln(64)-\ln(1)}$ | $S_S^{(90)}=10\!\cdot\!\frac{\ln(64)-\ln(s_{90})}{\ln(64)-\ln(1)}$ | $S_S^{(99)}=10\!\cdot\!\frac{\ln(64)-\ln(s_{99})}{\ln(64)-\ln(1)}$ |
| coreConc | $c$＝实测达标所用核心数（1~$N$, $N$=总核） | $S_C^{(60)}=10\!\cdot\!\frac{\ln(N)-\ln(c_{60})}{\ln(N)-\ln(1)}$ | $S_C^{(90)}=10\!\cdot\!\frac{\ln(N)-\ln(c_{90})}{\ln(N)-\ln(1)}$ | $S_C^{(99)}=10\!\cdot\!\frac{\ln(N)-\ln(c_{99})}{\ln(N)-\ln(1)}$ |

> **三档独立评定**：
> - 60% 带宽组：4 维度各自以最小参数打满 60% 带宽时的得分 → 反映"基础可用"易度
> - 90% 带宽组：4 维度各自以最小参数打满 90% 带宽时的得分 → 反映"高效实用"易度
> - 99% 带宽组：4 维度各自以最小参数打满 99% 带宽时的得分 → 反映"极致打满"易度
>
> 每个维度 $X$ 在三档下独立产出 $S_X^{(60)}, S_X^{(90)}, S_X^{(99)}$，不做平均，分别描述该维度在不同带宽目标下的易用性表现。
>
> **单维度总分**（若需聚合为单值）：$S_X = \frac{1}{3}\!\left(S_X^{(60)}+S_X^{(90)}+S_X^{(99)}\right)$
>
> **子项总分**（四维度等权）：$S = \frac{1}{4}(S_T+S_B+S_S+S_C)$
>
> **探测方式**：每个维度取其他三维度最大化固定，目标维度从最小值开始递增直到 $p\ge$ 目标阈值，记录此时 $X_p$。对 stride 仅测 s mod 64 ≠ 0 的场景。


---

### 3.2 编译友好

> 度量编译时能否自动消化硬件细节，减少用户手动调优的代价

#### api 编译友好

**2.a.1 api 编译友好** — 暂不度量

#### 编译效率

**2.b.1 编译效率** — 暂不度量

---

### 3.3 内存模型

> 度量在内存层级、离散访问、cache一致性等方面对用户的认知与编码代价

#### buffer 层级

**3.a.1 对齐颗粒度**

> 度量范围：芯片内多个可访问的 buffer，每个 buffer 的对齐颗粒度；
>
> 度量目标：内存对齐符合 C 语言约束（E: 不感知=10分；感知时 2**N 分档直接打分）； **与三档性能无关**

| 度量维度 | 可采集参数 | 60% 带宽利用率 | 90% 带宽利用率 | 99% 带宽利用率 |
| :-- | :-- | :-- | :-- | :-- |
| 各层级Buffer cacheline 对齐颗粒度 | $s$＝ buffer 硬对齐最小粒度 | $S_i^{(60)}=10\cdot\frac{\ln(64)-\ln(s)}{\ln(64)}$ | $S_i^{(90)}=10\cdot\frac{\ln(64)-\ln(s)}{\ln(64)}$ | $S_i^{(99)}=10\cdot\frac{\ln(64)-\ln(s)}{\ln(64)}$ |

> **评定**：
> - 每个 buffer 的cacheline 颗粒度
> - **单 buffer 分**（聚合）：$S_i = S_i^{(99)}$
> - **子项总分**：$S = \frac{1}{M}\sum_{i=1}^{M} S_i$（$M$ 个 buffer 取均值）



**3.a.2 bank/bankgroup**

> 度量目标：为了规避 Bank 冲突，软件开发人员往往需要付出极高的心智负担和代码可读性代价（如强行插入无意义的 Padding、破坏业务逻辑直观性）。
> 连续内存 BenchMark 访问带宽默认达成率 $p_0=B_{real,0}/B_{peak}$，若 $p_0 \ge 95\%$ 直接 10 分。
> 否则从"代码修改比例"和"圈复杂度"两个维度量化易用性。

| 度量维度 | 可采集参数 | 60% 带宽利用率 | 90% 带宽利用率 | 99% 带宽利用率 |
| :-- | :-- | :-- | :-- | :-- |
| 代码修改比例 | $\gamma$＝为达成目标带宽所修改的非业务代码行数 / 默认实现总代码行数 | $S_{\gamma}^{(60)}\!=\!10\!\cdot\!\frac{\ln(L_{total})-\ln(L_{c,60}+1)}{\ln(L_{total})}$ | $S_{\gamma}^{(90)}\!=\!10\!\cdot\!\frac{\ln(L_{total})-\ln(L_{c,90}+1)}{\ln(L_{total})}$ | $S_{\gamma}^{(99)}\!=\!10\!\cdot\!\frac{\ln(L_{total})-\ln(L_{c,99}+1)}{\ln(L_{total})}$ |
| 圈复杂度变化比 | $\Delta V$＝优化后圈复杂度 / 默认实现圈复杂度（$V$ 为标准 McCabe 圈复杂度） | $S_{V}^{(60)}\!=\!10\!\cdot\!\mathrm{clamp}\!\big(2-e^{\Delta V_{60}-1},\,0,\,1\big)$ | $S_{V}^{(90)}\!=\!10\!\cdot\!\mathrm{clamp}\!\big(2-e^{\Delta V_{90}-1},\,0,\,1\big)$ | $S_{V}^{(99)}\!=\!10\!\cdot\!\mathrm{clamp}\!\big(2-e^{\Delta V_{99}-1},\,0,\,1\big)$ |

> **三档独立评定**：每档独立产出得分，不做平均。
>
> **代码修改比例公式**（$\gamma = \frac{L_{c,p}+1}{L_{total}}$）：
> 修改比例越小（即用很少代码就能消除冲突）→ 分数越高
> $$S_{\gamma}^{(p)} = 10\cdot\frac{\ln(L_{total})-\ln(L_{c,p}+1)}{\ln(L_{total})}$$
> 其中 $L_{total}$ 为默认实现的业务代码总行数，$L_{c,p}$ 为达成目标档位 $p$ 修改/新增行数（+1 防 ln(0)）；零修改=10 分。
>
> **圈复杂度变化比公式**（$\Delta V = \frac{V_{opt}}{V_{base}}$）：
> 优化后圈复杂度相对原始实现的变化，$V$ 取 McCabe 圈复杂度（= 边数 - 节点数 + 2）
> $$S_{V}^{(p)} = 10\cdot\mathrm{clamp}\!\big(2-e^{\Delta V_{p}-1},\,0,\,1\big)$$
> - $\Delta V = 1$（复杂度不变）→ $2-e^0 = 1$→10 分
> - $\Delta V = 1.5$（复杂度增加 50%）→ $2-e^{0.5}\approx 0.35$→3.5 分
> - $\Delta V = 2$（复杂度翻倍）→ $2-e^1\approx -0.72$→0 分
> - 函数衰减特性：复杂度飙升时分数急剧下降，体现软件易维护性变差。
>
> **易用判定**：$p_0 \ge 95\%$ → 三档直接 10 分（无需修改、圈复杂度不变）
>
> **单档聚合**：$S^{(p)} = 0.6\,S_{\gamma}^{(p)} + 0.4\,S_{V}^{(p)}$
>
> **子项总分**（若需单值）：$S = \frac{1}{3}\sum_{p} S^{(p)}$
>
> **探测方式**：
> - 选取 Benchmark 算子默认实现测 $p_0$ 和 $V_{base}$（McCabe 圈复杂度静态分析）
> - 对每档目标带宽，根据 profiling 定位 bank 冲突热点，改写访问模式（调整 stride/插入 padding 等），记录：修改行数 $L_{c,p}$（区分业务行与 padding），优化后的圈复杂度 $V_{opt}$
> - 若某档默认即达标（$p_0 \ge p$），则 $L_{c,p}=0$ 且 $\Delta V=1$

**3.a.3 层级数**

> 度量目标：软件感知层级是越少越好；度量从 GM 到计算引擎的流水切换次数
> 统计四条搬运通路的流水切换数：GM→Vector、GM→Cube、Cube→Vector、Vector→Cube

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| GM→Vector 流水切换 | $P_{gm2vec}$＝GM 到 Vector 的流水切换次数 | $S_{gm2vec}=10\cdot\mathrm{clamp}\!\big(1-\frac{P_{gm2vec}}{P_{ref}},\,0,\,1\big)$ |
| GM→Cube 流水切换 | $P_{gm2cube}$＝GM 到 Cube 的流水切换次数 | $S_{gm2cube}=10\cdot\mathrm{clamp}\!\big(1-\frac{P_{gm2cube}}{P_{ref}},\,0,\,1\big)$ |
| Cube→Vector 流水切换 | $P_{cube2vec}$＝Cube 到 Vector 的流水切换次数 | $S_{cube2vec}=10\cdot\mathrm{clamp}\!\big(1-\frac{P_{cube2vec}}{P_{ref}},\,0,\,1\big)$ |
| Vector→Cube 流水切换 | $P_{vec2cube}$＝Vector 到 Cube 的流水切换次数 | $S_{vec2cube}=10\cdot\mathrm{clamp}\!\big(1-\frac{P_{vec2cube}}{P_{ref}},\,0,\,1\big)$ |

> $S = \frac{1}{4}\!\big(S_{gm2vec}+S_{gm2cube}+S_{cube2vec}+S_{vec2cube}\big)$
>
> **公式说明**：$P_{ref}=3$（单方向最多允许 3 次流水切换）；流水切换为 0 → 10 分；每增加 1 次切换扣约 3.3 分。
>
> **探测方式**：直接分析硬件搬运行为。

**3.a.4 容量**

> 度量目标：编程因容量而进行多种算法设计。统计 Benchmark 算子最大存活节点个数 × 达成三档性能时的 TileSize，与 buffer 容量（UB/L1/L0C）的比值；比值 ≤1 即容量内可容纳，无需分块=10 分；反之按比率度量。

| 度量维度 | 可采集参数 | 60% 带宽利用率 | 90% 带宽利用率 | 99% 带宽利用率 |
| :-- | :-- | :-- | :-- | :-- |
| 容量比值 | $\rho=\frac{N_{alive}\cdot T_{p}}{C_{buf}}$：$N_{alive}$=最大存活节点数，$T_p$=达成目标档位 $p$ 的 TileSize（Byte），$C_{buf}$=目标 buffer 容量 | $\rho_{60}=\frac{N_{alive}\cdot T_{60}}{C_{buf}}$ | $\rho_{90}=\frac{N_{alive}\cdot T_{90}}{C_{buf}}$ | $\rho_{99}=\frac{N_{alive}\cdot T_{99}}{C_{buf}}$ |
| 单 buffer 得分 | 若 $\rho\le 1$ 则容量满足，直接 10 分 | $S_{buf}^{(60)}=\min\!\big(10,\;\frac{10}{\rho_{60}}\big)$ | $S_{buf}^{(90)}=\min\!\big(10,\;\frac{10}{\rho_{90}}\big)$ | $S_{buf}^{(99)}=\min\!\big(10,\;\frac{10}{\rho_{99}}\big)$ |

> **三档独立评定**：每档独立产出得分，不做平均。
>
> **公式逻辑**：$\rho\le 1$（存活数据 ≤ 容量）→ 无需分块，单实现满分 10 分；$\rho>1$ → 必须分块/多分支，$S=\frac{10}{\rho}$，容量超出越多分数越低。
>
> **子项总分**：
> 1. 对每个 Benchmark 算子 $j$（共 $K$ 个，覆盖 FA/MM/CONV/Elementwise 等 Pattern），计算容量比值：$\rho_{buf,j}^{(p)} = \frac{N_{alive,j}\cdot T_{p,j}}{C_{buf}}$
> 4. $K$ 个算子按 weight 加权均值：$S = \frac{\sum_{j=1}^{K} w_j\rho_{buf,j}^{(p)}}{\sum_{j=1}^{K} w_j}$，其中 $w_j$ 取自静态 pattern 评分 
>
> **探测方式**：
> - $N_{alive,j}$ 从算子 $j$ 的数据流/存活分析中取最大并发存活张量个数
> - $T_{p,j}$ 为算子 $j$ 在带宽利用率 $p$ 下的 TileSize（沿用 1.a.1 实测值）
> - $C_{buf}$ 从芯片规格书读取 UB/L1/L0C 的 SRAM 容量（Byte）
> - $w_j$ 按「静态pattern 评分」中内存模型列



**3.a.5 层级间通路**

> 度量目标：内存层间可互访。度量 Benchmark 算子在各内存层级间（GM→UB, GM→L1, UB→GM, UB→L1, L0C→UB 等）完成数据搬运所付出的地址计算、参数填充、搬运指令消耗的代码行，与算子总代码行的比值；比值越小说明搬运通路越透明，易用性越高。

| 度量维度 | 可采集参数 | 60% 带宽利用率 | 90% 带宽利用率 | 99% 带宽利用率 |
| :-- | :-- | :-- | :-- | :-- |
| 搬运代码占比 | $\gamma=\frac{L_{dma}}{L_{total}}$：$L_{dma}$=各层间通路（GM↔UB, GM↔L1, UB↔L1, L0C↔UB…）完成地址计算、参数填充、搬运指令的非业务代码行数之和，$L_{total}$=算子总代码行数 | $\gamma_{60}=\frac{L_{dma,60}}{L_{total}}$ | $\gamma_{90}=\frac{L_{dma,90}}{L_{total}}$ | $\gamma_{99}=\frac{L_{dma,99}}{L_{total}}$ |
| 单算子得分 | $\gamma\le 0.05$（搬运代码占比≤5%）→ 10 分 | $S_j^{(60)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma_{60}}{0.3},\,0,\,1\big)$ | $S_j^{(90)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma_{90}}{0.3},\,0,\,1\big)$ | $S_j^{(99)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma_{99}}{0.3},\,0,\,1\big)$ |

> **三档独立评定**：每档独立产出得分，不做平均。
>
> **公式逻辑**：$\gamma$ 越小（搬运通路透明、代码自动推导）→ 分数越高。$\gamma\le 0.05$（搬运代码占比≤5%）→ 10 分；$\gamma=0.3$ → 0 分；线性衰减。
>
> **子项总分**：
> $$S = \frac{\sum_{j=1}^{K} w_j\cdot S_j}{\sum_{j=1}^{K} w_j}$$
> 其中 $K$ 个 Benchmark 算子（覆盖 FA/MM/CONV 等 Pattern），$w_j$ 取自「静态pattern 评分.xlsx」内存模型列（难=3, 中=2, 易=1）。
>
> **探测方式**：
> - 对每个 Benchmark 算子源码做 AST/行级静态统计，标记所有 GM↔UB, GM↔L1, UB↔L1, L0C↔UB 等层间通路的搬运编排代码（地址偏移计算、格式转换、搬运 API 调用、参数填充），汇总为 $L_{dma,p}$（各档带宽目标下）；$L_{total}$ 为算子总代码行数。
> - $\gamma_p = L_{dma,p}/L_{total}$，三档分别代入。



#### cache

**3.b.1 dcache 一致性**

> 度量目标：不感知 dcache 一致性。统计 Benchmark 算子中为维护一致性显式编写的代码行（cacheline 对齐、DCCI prefetch/invalidate 指令）占总代码行的比率；三档统一度量该比率。

| 度量维度 | 可采集参数 | 60% 带宽利用率 | 90% 带宽利用率 | 99% 带宽利用率 |
| :-- | :-- | :-- | :-- | :-- |
| 一致性代码占比 | $\gamma_{coh}=\frac{L_{coh}}{L_{total}}$：$L_{coh}$=为维护 dcache 一致性显式编写的代码行数（cacheline 对齐、DCCI 指令、prefetch/invalidate），$L_{total}$=算子总代码行数 | $S_j^{(60)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma_{coh,60}}{0.2},\,0,\,1\big)$ | $S_j^{(90)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma_{coh,90}}{0.2},\,0,\,1\big)$ | $S_j^{(99)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma_{coh,99}}{0.2},\,0,\,1\big)$ |

> **三档独立评定**：每档独立产出得分，不做平均。三档共用同一公式结构，行为一致。
>
> **公式逻辑**：$\gamma_{coh}$ 越小（一致性完全由硬件/运行时自动处理）→ 分数越高。$\gamma_{coh}=0$（无需用户感知）→ 10 分；$\gamma_{coh}\ge 0.2$ → 0 分；线性衰减。
>
> **子项总分**：$$S = \frac{\sum_{j=1}^{K} w_j\cdot\frac{1}{3}\sum_{p} S_j^{(p)}}{\sum_{j=1}^{K} w_j}$$
> 其中 $K$ 个 Benchmark 算子，$w_j$ 取自「静态pattern 评分.xlsx」内存模型列（难=3, 中=2, 易=1）。
>
> **探测方式**：对源码做 AST 静态统计，匹配 DCCI prefetch/invalidate、cacheline 对齐相关的显式调用与偏移处理代码，汇总为 $L_{coh}$；$L_{total}$ 为算子总代码行；三档分别记录。



**3.b.2 hint 行为控制**

> 度量目标：尽量少的 hint 行为（无需 hint 达最优=10分，每增1类 hint 扣2分；无 hint 性能差距分档：<5%=10,5-15%=8,15-30%=5,>30%=≤3）

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| hint 种类数 | $N_{hint}$＝硬件提供的需用户感知的 hint 种类数（预取 hint/persistent hint 等），统计 Benchmark 算子中实际使用的 hint 种类 | $S_{hint}=\mathrm{clamp}\!\big(10-2N_{hint},\,0,\,10\big)$ |
| 无 hint 性能差距 | $gap=\frac{T_{no\_hint}}{T_{with\_hint}}-1$：不使用 hint vs 使用 hint 的性能比值差距 | $S_{gap}=\begin{cases}10,&gap<5\%\\ 8,&5\%\le gap<15\%\\ 5,&15\%\le gap<30\%\\ 3,&gap\ge 30\%\end{cases}$ |


> $S = 0.45\,S_{hint} + 0.55\,S_{gap} $
>
> **探测方式**：
> - $N_{hint}$：统计硬件提供 hint 的单元的种类
> - $gap$：对每个算子分别跑无 hint/有 hint 的 A/B 对比，取 10 个算子性能差距的均值作为 $gap$

**3.b.3  亲Die部署**

> 度量目标：尽量不感知拓扑行为。从 Die 感知和 Cluster 感知两个维度扣分：完全无感知=10分；必须感知 Cluster→5分；两者同时感知→0分。

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| Cluster 感知 | $f_{cluster}$＝Benchmark 算子是否必须感知 Cluster 核映射（0=不感知, 1=必须感知） | 若 $f_{cluster}=1$ → 扣 5 分 |

> $S = 10 -  5\cdot f_{cluster}$，$\mathrm{clamp}$ 到 $[0,10]$
>
> **公式逻辑**：
> - 完全不感知（f_{cluster}=0$）→ 10 分
> - 仅感知 Cluster（f_{cluster}=1$）→ 5 分
> **探测方式**：对 Benchmark 算子源码做静态分析，判断是否包含 Cluster 核映射代码（$f_{cluster}$），均为二值判定，  然后基于算子个数取均值。

**3.b.4 容量**

> 度量目标：尽量不感知 cache 容量限制（E: 二进制/栈与 cache 比值<1→10分，每超10%扣1分；ICache miss<1%=10,1-5%=8,5-15%=5,>15%=≤3）

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 二进制/栈比 | $r_{bin}$＝(二进制+stack)/ICache/DCache 大小，$r_{bin}\le1$ | $S_{ratio}=10\cdot\mathrm{clamp}(1-\frac{r_{bin}-1}{0.1},0,1)$（或>1 按每10%扣1分） |
| ICache miss 率 | $m_{ic}$＝ICache miss 率（FA/MM/CONV 算子） | $S_{miss}$: <1%→10, 1-5%→8, 5-15%→5, >15%→3 |


> $S = 0.40\,S_{ratio} + 0.6\,S_{miss} $

---

### 3.4 计算模型

> 度量在计算指令原子性、完备性、参数复杂度等方面对用户的编程负担

#### 计算指令

**4.a.1 计算指令原子性**

> 度量目标：计算指令原子化；无状态残留；无上下文依赖。
> 统计头文件 include 中声明的全部计算 API，逐条按 5 项原子性规则判定，计算违反项的 API 占总 API 数的比率。

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 原子性违反比率 | $\tau = \frac{N_{viol}}{N_{api}}$：$N_{api}$=头文件中声明的计算指令 API 总数，$N_{viol}$=违反 5 项原子性规则（非1对1映射/有状态残留/跨2流水/寄存器相关/功能互斥或缺陷）中至少 1 项的 API 个数 | $S_{atom}=10\cdot(1-\tau)$ |

> $S = \,S_{atom} $
>
> **探测方式**：解析头文件 include 中所有计算指令 API 声明，逐条对照 5 项判定（$N_1$~$N_5$），统计 $N_{viol}$ 和 $N_{api}$；$L_c$ 为 Benchmark 算子中为补齐原子性缺陷而额外写的代码行数。

**4.a.2 计算指令性能一致性**

> 度量目标：相同功能下指令选择单一。统计单一硬件指令封装出不同 API 的比例：每条硬件指令被封装为多个 API 则增加选择分支，单一映射为最优。

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 一指令多 API 比率 | $\eta=\frac{N_{api\_dup}}{N_{hw}}$：$N_{hw}$=头文件 include 中引用的底层硬件指令总数，$N_{api\_dup}$=同一硬件指令对应多个上层 API 的指令条数 | $S_{\eta}=10\cdot(1-\frac{N_{api\_dup}}{N_{hw}})$ |
| 友商对标 | $R=\eta_{self}/\eta_{baseline}$（一指令多 API 比率比，越小越优） | $S_{comp}=10\cdot\mathrm{clamp}\!\big(\frac{1}{R},\,0,\,1\big)$ |

> $S = 0.65\,S_{\eta} + 0.35\,S_{comp}$
>
> **公式逻辑**：$\eta$ 越小（每条硬件指令只对应 1 个 API）→ 分数越高。$\eta=0$（全部 1:1 映射）→ 10 分；$\eta=1$（每条硬件指令都有多个 API 封装）→ 0 分。
>
> **探测方式**：解析头文件 include 中所有计算 API 声明与底层硬件指令的映射关系，统计 $N_{hw}$ 和 $N_{api\_dup}$（同一硬件指令对应 ≥2 个不同 API 的指令条数）。

**4.a.3 指令参数**

> 度量目标：指令参数个数少、语义清晰、非必要参数少。从非必选参数比率和枚举暴露度两个维度度量。

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 非必选参数比率 | $\pi_i=\frac{P_{optional,i}}{P_{total,i}}$：对第 $i$ 个 API，$P_{total,i}$=总参数数，$P_{optional,i}$=非必选参数数（可选/有默认值/非关键模板参数） | 所有 $N$ 个 API 取均值：$S_{\pi}=\frac{1}{N}\sum_{i}10\cdot\mathrm{clamp}\!\big(1-\frac{\pi_i}{0.5},\,0,\,1\big)$ |
| 枚举暴露度 | $\varepsilon=\frac{N_{enum\_used}}{N_{enum\_def}}$：$N_{enum\_def}$=接口中定义的枚举选项总数，$N_{enum\_used}$=Benchmark 算子中实际使用的枚举选项数。枚举选项对应硬件指令行为 | $S_{\varepsilon}=10\cdot\mathrm{clamp}\!\big(\frac{\varepsilon-0.3}{0.7},\,0,\,1\big)$ |

> $S = 0.5\,S_{\pi} + 0.5\,S_{\varepsilon}$
>
> **公式逻辑**：
> - 非必选参数比率 $\pi_i$ 越低越好（$\pi_i=0$→10分，$\pi_i\ge0.5$→0分）
> - 枚举暴露度 $\varepsilon$ 越高越好（实际使用的枚举占接口定义的比例越高，说明接口暴露的枚举都是有用的）：$\varepsilon\le0.3$→0分（大量冗余枚举），$\varepsilon\ge1.0$→10分（接口定义的枚举全部被使用）
>
> **探测方式**：
> - 解析头文件中所有计算 API 的参数列表，统计 $P_{total,i}$ 和 $P_{optional,i}$
> - 统计接口中所有枚举定义 $N_{enum\_def}$，对 Benchmark 算子源码扫描实际使用的枚举值 $N_{enum\_used}$

**4.a.4 计算指令的完备性**

> 度量目标：业务表达与业界竞品对齐、覆盖关键场景（竞品对齐率 = 硬件实现对齐 + 软件仿真对齐）

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 硬件实现对齐率 | $r_{hw}=\frac{\text{自研硬件可直接实现对齐的竞品 API 数}}{\text{竞品总硬件 API 数}}$ | $S_{hw}=10\,r_{hw}$ |
| SIMT 实现对齐率 | $r_{simt}=\frac{\text{自研 SIMT 模式可对齐的竞品 API 数}}{\text{竞品总 API 数}}$ | $S_{simt}=10\,r_{simt}$ |
| 软件仿真对齐率 | $r_{sw}=\frac{\text{需软件仿真才能对齐的竞品 API 数}}{\text{竞品总 API 数}}$ | $S_{sw}=10\,(1-r_{sw})$ |
| cast/atomic 完备度 | $cov=\frac{\text{提供的cast 转换API }}{\text{cast 数据类型组合}}$ | $S_{cover}=10\,cov$ |

> **精品对齐率**：$r_{align} = r_{hw} + r_{simt} + r_{sw}$，表示自研 API 对竞品 API 的总覆盖率（$r_{hw}+r_{simt}$ 为硬件/SIMT 可直接对齐比例，$r_{sw}$ 为需软件仿真的兜底比例）
>
> $S = 0.25\,S_{hw} + 0.25\,S_{simt} + 0.15\,S_{sw} + 0.35\,S_{cover}$
>
> **权重说明**：硬件实现对齐和 SIMT 对齐各占 25%（硬件能力直接影响易用性），软件仿真占 15%（兜底方案、代价较高），场景覆盖度占 35%（与原始一致）。
>
> **探测方式**：
> - 以竞品（NV 等）公开 API 列表为基准，逐条对照自研 API 的实现方式：
>   - 硬件直通：自研硬件指令可直接完成该 API 功能 → $r_{hw}$
>   - SIMT 模式：需要 SIMT 编程模型配合硬件实现 → $r_{simt}$
>   - 软件仿真：硬件不直接支持，需纯软件仿真 → $r_{sw}$
> - $r_{hw} + r_{simt} + r_{sw} \le 1$（三者之和 ≤ 竞品总 API 的 100%）

#### 随路计算

**4.b.1 随路计指令性能一致性**

> 度量目标：以 API 为粒度，配置随路与非随路两种模式，度量二者性能比值；比值越接近 1 说明随路计算性能波动越小，即同一 API 不管是否配置随路计算都能稳定发挥性能

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 随路/非随路性能比值 | $\rho_i=\frac{T_{fused,i}}{T_{no\_fused,i}}$：对第 $i$ 个 API，$T_{fused,i}$=配置随路计算的执行时间，$T_{no\_fused,i}$=不配置随路计算的执行时间 | $S_i = 10\cdot\mathrm{clamp}\!\big(1-\frac{\|\rho_i-1\|}{0.25},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{N}\sum_{i=1}^{N} S_i$，$N$ 为支持随路计算的 API 总数
>
> **公式逻辑**：$\rho_i=1$（随路与非随路性能一致）→ 满分 10 分；每偏离 2.5\% 扣 1 分，$S_i = 10 - \frac{\|\rho_i-1\|}{0.025}$，$\|\rho_i-1\|\ge 25\%$ 时归零。逐 API 打分取均值。
>
> **探测方式**：对每个支持随路计算的 API，分别跑随路模式与非随路模式的 Benchmark，记录各自的执行时间，计算比值 $\rho_i$。

**4.b.2 低bit量化**

> 度量目标：度量低bit量化场景下，硬件/软件对低bit数据类型、精度、Layout、MFU等方面的易用性。从MMA指令原生支持、指令精度、量化/转换指令、Scale元数据管理、典型MFU达成率、Subnorm/Inf/NaN支持六个维度度量。

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| MMA 指令原生支持 | $r_{mma}=\frac{N_{native\_matmul}}{N_{required}}$：$N_{native\_matmul}$=任意低bit类型参与 MMA 计算且输出为任意低bit的指令组合数，$N_{required}$=业务所需组合总数 | $S_{mma}=10\cdot\mathrm{clamp}\!\big(r_{mma},\,0,\,1\big)$ |
| 指令精度 | $\varepsilon_{ulp}$＝低bit指令平均 ULP 误差；满足 V2.0 ULP 标准 → 10 分 | $S_{ulp}=10\cdot\mathrm{clamp}\!\big(1-\frac{\max(\varepsilon_{ulp}-2.0,\,0)}{2.0},\,0,\,1\big)$ |
| 量化/转换指令 | $c_{conv}$＝从源类型转到目标类型所需中间转换次数；硬件支持 RTN/SR 舍入模式 = $+1$ 分 | $S_{conv}=10\cdot\frac{1}{2^{c_{conv}-0}} + \mathrm{round\_bonus}$（零转换=10 分，每多1次→半衰，RTN/SR 各+0.5） |
| Scale 元数据管理 | $\gamma_{scale}=\frac{L_{scale\_extra}}{L_{total}}$：为处理 per-channel/per-block/tensor-wise 等 scale layout 所需额外代码行占比 | $S_{scale}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma_{scale}}{0.15},\,0,\,1\big)$ |
| 典型量化计算 MFU 达成率 | $r_{mfu}=\frac{MFU_{quant}}{MFU_{fp16\_ref}}$：典型量化计算在考虑量化通道后的 MFU 与 FP16 基准 MFU 之比 | $S_{mfu}=10\cdot\mathrm{clamp}\!\big(r_{mfu},\,0,\,1\big)$ |
| Subnorm/Inf/NaN 支持 | $r_{ieee}$＝对 Subnorm/Inf/NaN 行为与 GPU IEEE 标准一致性比例 | $S_{ieee}=10\cdot r_{ieee}$ |

> **子项总分**：$S = 0.25\,S_{mma} + 0.15\,S_{ulp} + 0.15\,S_{conv} + 0.15\,S_{scale} + 0.20\,S_{mfu} + 0.10\,S_{ieee}$
>
> **公式逻辑**：
> - MMA 原生支持(0.25)：任意低bit类型参与 matmul 计算并输出低bit 的能力是最核心的易用指标
> - 指令精度(0.15)：满足 V2.0 ULP 标准即满分，超出线性扣分
> - 量化/转换(0.15)：期望直接转换为目标类型，每增加 1 次中间转换易用性减半；硬件支持 RTN/SR 舍入加分
> - Scale 元数据(0.15)：支持随路 scale 转换，per-channel/per-block 等 layout 需要额外代码越少越好
> - MFU 达成率(0.20)：量化通道对性能的影响，MFU 越接近 FP16 基准越好
> - IEEE 兼容(0.10)：Subnorm/Inf/NaN 行为与 GPU 一致则满分
>
> **探测方式**：
> - $N_{native\_matmul}$ 从指令集手册统计低bit MMA 指令组合；$N_{required}$ 从业务场景分析
> - $\varepsilon_{ulp}$ 通过 ULP 误差测试框架测量各低bit 指令输出 vs 参考实现
> - $c_{conv}$ 从类型系统分析源→目标的转换路径；RTN/SR 从指令集规格确认
> - $\gamma_{scale}$ 对 Benchmark 算子源码 AST 分析，统计 scale/per-channel/per-block 相关额外代码
> - $r_{mfu}$ 通过 Profiling 采集量化计算 MFU 与 FP16 基准 MFU
> - $r_{ieee}$ 通过 Subnorm/Inf/NaN 边界用例测试，对比 GPU 行为

---

### 3.5 控制模型

> 度量在多流水编排、同步原语、生产者消费者模型等方面的控制复杂度

#### 多pipeline 并行

**5.a.1 多pipeline 并行**

> 度量目标：度量 Benchmark 算子运行时实际切换 pipeline（或 stage）次数与源代码表达的 pipeline 切换次数（剔除 for 循环中重复切换）的比值，越接近 1 说明运行时无额外流水切换开销，编译器/硬件能高效融合流水

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 运行时/源码流水切换比 | $\gamma_i=\frac{P_{runtime,i}}{P_{src,i}}$：对第 $i$ 个 Benchmark 算子，$P_{runtime,i}$=运行时实际发生的 pipeline/stage 切换次数，$P_{src,i}$=源代码表达的 pipeline/stage 切换次数（已剔除 for 循环内重复切换，只计静态切换点） | $S_i = 10\cdot\mathrm{clamp}\!\big(2-\gamma_i,\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：$\gamma_i=1$（运行时切换数 = 源码切换数）→ 满分 10 分；$\gamma_i\ge 2$（运行时切换数是源码的 2 倍以上）→ 0 分；区间内线性衰减。比值越小说明编译器/硬件能有效融合流水级，减少运行时流水切换。
>
> **探测方式**：
> - $P_{src,i}$：对 Benchmark 算子源码做 AST 静态分析，统计 pipeline/stage 切换语句，剔除 for 循环内的重复切换点（同一语句在循环内只计 1 次）
> - $P_{runtime,i}$：通过硬件 profiling/探针在运行时统计实际发生的流水切换次数
#### 同步

**5.b.1 核间同步**

> 度量目标：统计 Benchmark mix 算子中同步相关代码行（含同步 ID 分配、同步参数填充、CrossCore SetFlag/WaitFlag 调用等）占算子总代码行的比例，越小越好

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 同步代码行占比 | $r_i=\frac{L_{sync,i}}{L_{total,i}}$：对第 $i$ 个 Benchmark mix 算子，$L_{sync,i}$=同步相关代码行数（同步 ID 分配/参数填充/SetFlag/WaitFlag 调用等），$L_{total,i}$=算子总代码行数 | $S_i = 10\cdot\mathrm{clamp}\!\big(1-\frac{r_i}{0.3},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark mix 算子个数
>
> **公式逻辑**：$r_i=0$（无同步代码）→ 满分 10 分；$r_i\ge 30\%$ → 0 分；区间内线性衰减。同步代码占比越小，说明核间同步越轻量、硬件/运行时自动处理越多。
>
> **探测方式**：对每个 Benchmark mix 算子源码做 AST 静态分析，统计所有核间同步相关代码行（同步 ID 初始化/分配、参数填充配置、CrossCore SetFlag/WaitFlag 调用点、同步前后的编排代码），汇总为 $L_{sync,i}$；$L_{total,i}$ 为算子总代码行数。

**5.b.2 核内同步**

> 度量目标：参照 5.b.1，统计 Benchmark 算子中核内同步相关代码行（含同步 ID 分配、对消处理、异常处理、ID 管理等）占算子总代码行的比例，越小越好

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 核内同步代码行占比 | $r_i=\frac{L_{intra\_sync,i}}{L_{total,i}}$：对第 $i$ 个 Benchmark 算子，$L_{intra\_sync,i}$=核内同步相关代码行数（同步 ID 初始化分配/严格对消/残留异常处理/ID 管理等），$L_{total,i}$=算子总代码行数 | $S_i = 10\cdot\mathrm{clamp}\!\big(1-\frac{r_i}{0.25},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：$r_i=0$（无核内同步代码）→ 满分 10 分；$r_i\ge 25\%$ → 0 分；区间内线性衰减。核内同步代码占比越小，说明核内同步越轻量。
>
> **探测方式**：对每个 Benchmark 算子源码做 AST 静态分析，统计所有核内同步相关代码行（同步 ID 初始化/分配、对消操作、残留异常检测/处理、ID 生命周期管理），汇总为 $L_{intra\_sync,i}$；$L_{total,i}$ 为算子总代码行数。

#### 生产者消费者模型

**5.c.1 生产者消费者模型**

> 度量目标：参照 5.a.1/5.b.1，统计 Benchmark 算子中表达生产者消费者模型所需代码行（含模型中参数配置、同步编排代码）占算子总代码行的比例，越小越好

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 生产消费模型代码行占比 | $r_i=\frac{L_{pc,i}}{L_{total,i}}$：对第 $i$ 个 Benchmark 算子，$L_{pc,i}$=表达生产者消费者模型所需的代码行数（同步控制编排/内存层级编排/模型参数配置等），$L_{total,i}$=算子总代码行数 | $S_i = 10\cdot\mathrm{clamp}\!\big(1-\frac{r_i}{0.35},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：$r_i=0$（无需显式表达生产者消费者模型）→ 满分 10 分；$r_i\ge 35\%$ → 0 分；区间内线性衰减。代码占比越小，说明模型表达越简洁、硬件/运行时自动管理越多。
>
> **探测方式**：对每个 Benchmark 算子源码做 AST 静态分析，统计生产者消费者模型相关代码行（同步控制编排/内存层级间的数据传递编排/模型参数填充与配置），汇总为 $L_{pc,i}$；$L_{total,i}$ 为算子总代码行数。

#### Scalar

**5.d.1 Scalar bound**

> 度量目标：在 60\% / 90\% / 99\% 三档性能目标下，从运行时间和代码占比两个维度度量 Scalar 开销

| 度量维度 | 可采集参数 | 60% 带宽利用率 | 90% 带宽利用率 | 99% 带宽利用率 |
| :-- | :-- | :-- | :-- | :-- |
| Scalar 与最大 pipe 时间比值 | $\gamma^{(p)}=\frac{T_{scalar}^{(p)}}{T_{maxPipe}^{(p)}}$：$T_{scalar}^{(p)}$=性能档位 $p$ 下 Scalar pipeline 的执行时间，$T_{maxPipe}^{(p)}=\max\!\big(T_{vec}^{(p)},\,T_{cube}^{(p)},\,T_{dma}^{(p)},\dots\big)$ | $S_{\gamma}^{(60)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma^{(60)}-1}{0.05},\,0,\,1\big)$ | $S_{\gamma}^{(90)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma^{(90)}-1}{0.05},\,0,\,1\big)$ | $S_{\gamma}^{(99)}=10\cdot\mathrm{clamp}\!\big(1-\frac{\gamma^{(99)}-1}{0.05},\,0,\,1\big)$ |
| Scalar 指令代码占比 | $r_{code,i}^{(p)}=\frac{L_{scalar,i}^{(p)}}{L_{total,i}^{(p)}}$：对第 $i$ 个 Benchmark 算子，$L_{scalar,i}^{(p)}$=Scalar 指令代码行数（剔除搬运、计算指令后的 Scalar 相关代码，包含传参/分支/控制逻辑等），$L_{total,i}^{(p)}$=算子总代码行数 | $S_{code,i}^{(60)}=10\cdot\mathrm{clamp}\!\big(1-\frac{r_{code,i}^{(60)}}{0.3},\,0,\,1\big)$ | $S_{code,i}^{(90)}=10\cdot\mathrm{clamp}\!\big(1-\frac{r_{code,i}^{(90)}}{0.3},\,0,\,1\big)$ | $S_{code,i}^{(99)}=10\cdot\mathrm{clamp}\!\big(1-\frac{r_{code,i}^{(99)}}{0.3},\,0,\,1\big)$ |

> **三档独立评定**：每档独立产出得分，不做平均。
>
> **公式逻辑**：
> - 时间维度 $\gamma$：$\gamma\le 1$（Scalar 执行时间 ≤ 其他 pipeline 最大值）→ 满分 10 分；$\gamma-1$ 每超出 5\% 扣 1 分
> - 代码维度 $r_{code}$：0→满分 10 分，$\ge 30\%$→0 分，线性衰减
>
> **单档聚合**：$S^{(p)} = 0.55\,S_{\gamma}^{(p)} + 0.45\,\frac{1}{K}\sum_{i=1}^{K} S_{code,i}^{(p)}$
>
> **子项总分**（若需单值）：$S = \frac{1}{3}\big(S^{(60)}+S^{(90)}+S^{(99)}\big)$
>
> **探测方式**：
> - 时间维度：对 Benchmark 算子在三档性能目标下运行，通过硬件 profiler 采集各 pipeline 执行时间，取其他 pipeline 最大值作为 $T_{maxPipe}^{(p)}$
> - 代码维度：对算子源码做 AST 静态分析，提取 Scalar pipeline 相关代码行（剔除 DMA 搬运指令、Vector/Cube 计算指令后，剩余的控制逻辑/分支/传参/ID 管理等代码），汇总为 $L_{scalar,i}^{(p)}$

#### 头开销

**5.e.1 头开销**

> 度量目标：度量 Benchmark 算子从开始执行到第一条搬运指令的时间占算子总时间的比值；度量第一条搬运指令之前所需的代码行占算子总代码行的比值；两者均越小越好

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 首搬运时延占比 | $r_{time,i}=\frac{T_{head,i}}{T_{kernel,i}}$：对第 $i$ 个算子，$T_{head,i}$=算子开始执行到第一条搬运指令的时间，$T_{kernel,i}$=算子总执行时间 | $S_{time,i}=10\cdot\mathrm{clamp}\!\big(1-\frac{r_{time,i}}{0.2},\,0,\,1\big)$ |
| 首搬运代码占比 | $r_{code,i}=\frac{L_{head,i}}{L_{total,i}}$：对第 $i$ 个算子，$L_{head,i}$=第一条搬运指令出现之前的代码行数（初始化/配置/参数填充等），$L_{total,i}$=算子总代码行数 | $S_{code,i}=10\cdot\mathrm{clamp}\!\big(1-\frac{r_{code,i}}{0.25},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K}\big(0.55\,S_{time,i} + 0.45\,S_{code,i}\big)$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：
> - 首搬运时延占比 $r_{time}$：0→满分 10 分，≥20\%→0 分，线性衰减
> - 首搬运代码占比 $r_{code}$：0→满分 10 分，≥25\%→0 分，线性衰减
> - 时间维度权重 0.55 高于代码维度 0.45（运行时开销直接影响算子性能）
>
> **探测方式**：
> - $T_{head,i}$、$T_{kernel,i}$：通过硬件 profiler/Trace 采集算子执行的时序信息
> - $L_{head,i}$、$L_{total,i}$：对算子源码做 AST 静态分析，定位第一条搬运指令所在行号，统计该行之前的代码行数

---

### 3.6 访存模型

> 度量在数据搬运地址计算、格式转换、tiling参数等方面的访存编码代价

#### 访存指令

**6.a.1 分型**

> 度量目标：度量 Benchmark 算子中实现数据搬运所花费的代码行（用于填充搬运指令入参的所有代码行，含地址偏移/格式转换/参数填充等）占算子总代码行的比值，越小越好

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 搬运代码行占比 | $r_i=\frac{L_{dma\_args,i}}{L_{total,i}}$：对第 $i$ 个 Benchmark 算子，$L_{dma\_args,i}$=填充搬运指令入参的代码行数（地址偏移计算/格式转换配置/搬运指令参数填充/ND layout 转换等），$L_{total,i}$=算子总代码行数 | $S_i = 10\cdot\mathrm{clamp}\!\big(1-\frac{r_i}{0.35},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：$r_i=0$（无搬运入参代码）→ 满分 10 分；$r_i\ge 35\%$ → 0 分；区间内线性衰减。搬运代码占比越小，说明访存指令越简洁、搬运入参越少。
>
> **探测方式**：对每个 Benchmark 算子源码做 AST 静态分析，统计所有搬运指令相关的入参填充代码行（地址偏移计算/stride 配置/格式转换参数/ND layout 转换/搬运 API 调用及参数填充），汇总为 $L_{dma\_args,i}$；$L_{total,i}$ 为算子总代码行数。

**6.a.2 非结构化访存**

> 度量目标：度量 Benchmark 算子中地址计算代码行（含 Gather/Scatter 等离散访问的偏移计算、stride 换算、非线性索引计算等）占算子总代码行的比值，越小越好

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 地址计算代码行占比 | $r_i=\frac{L_{addr,i}}{L_{total,i}}$：对第 $i$ 个 Benchmark 算子，$L_{addr,i}$=地址计算相关代码行数（Gather/Scatter 偏移计算/stride 换算/非线性索引/地址掩码等），$L_{total,i}$=算子总代码行数 | $S_i = 10\cdot\mathrm{clamp}\!\big(1-\frac{r_i}{0.3},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：$r_i=0$（无需手动地址计算）→ 满分 10 分；$r_i\ge 30\%$ → 0 分；区间内线性衰减。地址计算代码占比越小，说明硬件/运行时自动处理地址换算越多，用户编码代价越低。
>
> **探测方式**：对每个 Benchmark 算子源码做 AST 静态分析，统计所有地址计算相关代码行（Gather/Scatter 偏移/stride 换算/非线性索引/地址掩码/边界条件判断），汇总为 $L_{addr,i}$；$L_{total,i}$ 为算子总代码行数。

#### 申请与管理

**6.b.1 申请与管理**

> 度量目标：度量 Benchmark 算子中用于分配 UB、L1 等不同内存空间所花费的代码行（含申请/释放/绑定/生命周期管理等）占算子总代码行的比值，越小越好

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 内存管理代码行占比 | $r_i=\frac{L_{alloc,i}}{L_{total,i}}$：对第 $i$ 个 Benchmark 算子，$L_{alloc,i}$=内存空间分配相关代码行数（UB/L1 等 buffer 的申请/释放/绑定/生命周期管理/地址映射配置），$L_{total,i}$=算子总代码行数 | $S_i = 10\cdot\mathrm{clamp}\!\big(1-\frac{r_i}{0.25},\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：$r_i=0$（无需手动分配内存空间）→ 满分 10 分；$r_i\ge 25\%$ → 0 分；区间内线性衰减。内存管理代码占比越小，说明硬件/运行时自动管理越多。
>
> **探测方式**：对每个 Benchmark 算子源码做 AST 静态分析，统计所有内存空间分配管理相关代码行（UB/L1 等 buffer 的申请分配/释放回收/绑定绑定/生命周期管理/地址映射/内存空间选择切换），汇总为 $L_{alloc,i}$；$L_{total,i}$ 为算子总代码行数。

---

### 3.7 调试调优

> 度量在错误异常处理、芯片断点调试、可观测性、性能度量等方面的调试调优能力

#### 错误异常处理

**7.a.1 异常类型覆盖率**

> 度量目标：衡量异常模型中定义的硬件异常是否具有对应的检测与处理机制
>
> $$ 异常类型覆盖率 = \frac{\text{具备检测机制的异常类型数}}{\text{异常模型定义的异常类型总数}} \times 100\% $$

$$ r_{cov} = \frac{\text{有检测+处理机制的异常类型数}}{\text{异常模型定义的硬件异常类型总数}} $$
$$ S_{cov} = 10\,r_{cov} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{cov}$ | $S_{cov}=10\,r_{cov}$ | 规格书 vs 实现逐项验证 |

> $S = S_{cov}$

**7.a.2 异常检出率**

> 度量目标：故障注入后正确检测的比例
>
> $$ 异常检出率 = \frac{\text{被正确检测的故障注入次数}}{\text{有效故障注入总次数}} \times 100\% $$

$$ p_{det} = \frac{\text{正确检测的注入数}}{\text{注入总数}} $$
$$ S_{det} = 10\,p_{det} $$

| 配套指标 | 计算方法 | 说明 |
| :-- | :-- | :-- |
| 漏报率 $p_{fn}$ | 未检测到的有效故障次数 / 有效注入次数 | 衡量异常未被发现的风险 |
| 误报率 $p_{fp}$ | 无故障却产生异常告警的次数 / 正常运行次数 | 衡量错误告警干扰 |
| 重复上报率 $p_{dup}$ | 同一根因产生的冗余事件数 / 该根因总事件数 | 衡量错误风暴和日志噪声 |
| SDC 漏检率 $p_{sdc}$ | 未上报的错误结果次数 / 错误结果总次数 | 芯片数据正确性红线指标 |

$$ S_{noise} = 10\left(1 - \frac{p_{fn} + p_{fp} + p_{dup} + p_{sdc}}{4}\right) $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $p_{det}$、$p_{fn}$、$p_{fp}$、$p_{dup}$、$p_{sdc}$ | $S = 0.60\,S_{det} + 0.40\,S_{noise}$ | 故障注入测试 |

**7.a.3 异常上报完整率与准确率**

> 度量目标：上报信息的必选字段完整率及字段值与真实状态的一致性
>
> 必选字段典型包括：芯片/Die/Device/Core标识、异常模块/类型/错误码/严重程度、发生时间/首次异常时间/异常计数、PC/指令地址/关键寄存器/流水状态、访问地址/地址空间/访问类型/访问长度、ECC Syndrome/NoC Route/DMA Channel、首因标识/次生异常标识/关联事件ID、复位或任务终止前关键状态快照
>
> $$ 异常信息完整率 = \frac{\text{实际上报的有效必选字段数}}{\text{该异常规定的必选字段总数}} \times 100\% $$
>
> $$ 字段准确率 = \frac{\text{值与故障注入及硬件真实状态一致的字段数}}{\text{实际上报字段数}} \times 100\% $$

$$ c_{comp} = \frac{\text{必选字段完备的上报次数}}{\text{上报总次数}} $$
$$ c_{acc} = \frac{\text{字段值与真实状态一致的上报次数}}{\text{上报总次数}} $$
$$ S_{comp\_field} = 10\,c_{comp},\quad S_{acc} = 10\,c_{acc} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $c_{comp}$、$c_{acc}$ | $S = 0.55\,S_{acc} + 0.45\,S_{comp\_field}$ | 字段验证+状态比对测试 |

**7.a.4 首因识别率**

> 度量目标：级联异常中正确识别首个硬件根因的比例（例如：DMA 非法地址可能进一步引起 NoC 返回错误、AICore 取数超时、任务超时和 Device 复位）
>
> $$ 首因识别率 = \frac{\text{正确识别首个硬件根因的级联异常案例数}}{\text{级联异常案例总数}} \times 100\% $$

$$ r_{root} = \frac{\text{正确识别首因的级联事件数}}{\text{级联事件总数}} $$
$$ S_{root} = 10\,r_{root} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{root}$ | $S_{root}=10\,r_{root}$ | 故障注入+级联场景测试 |

> $S = S_{root}$

#### 芯片调试断点

**7.b.1 断点类型覆盖率**

> 度量目标：已支持断点类型数 / 目标场景要求的断点类型总数
>
> $$ 断点类型覆盖率 = \frac{\text{已支持断点类型数}}{\text{目标场景要求的断点类型总数}} \times 100\% $$

| 断点类型 | 主要用途 | NPU 典型场景 |
| :-- | :-- | :-- |
| 指令地址断点 | 执行到指定 PC 时暂停 | 定位 Scalar、Vector、Cube 指令 |
| 源码行断点 | 在目标源码语句暂停 | AscendC 源码级调试 |
| 函数断点 | 进入指定函数时暂停 | 定位 Kernel 内部函数调用 |
| 条件断点 | 条件满足时暂停 | `index == 1024`、循环第N次 |
| 数据断点/Watchpoint | 指定地址被读写时暂停 | 定位越界、非法修改、数据踩踏 |
| 异常断点 | 指定异常发生时暂停 | 非法指令、地址异常、同步超时 |
| Kernel 入口/出口断点 | Kernel 开始或结束时暂停 | 检查参数、初始状态和输出 |
| 同步断点 | 到达 Barrier 或 Event 时暂停 | 定位核间同步和死锁 |
| 指定核断点 | 只暂停指定 Core 或 Block | 多核并行问题定位 |
| 全局断点 | 多个核协调暂停 | 分析共享状态和多核一致性 |
| 命中次数断点 | 第 N 次经过目标位置时暂停 | 循环和重复 Kernel 定位 |
| 时间/超时断点 | 执行超过阈值时暂停 | 定位流水阻塞和死循环 |

$$ r_{bpcov} = \frac{\text{已支持断点类型数}}{\text{场景要求的断点类型总数（12类）}} $$
$$ S_{bpcov} = 10\,r_{bpcov} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{bpcov}$ | $S_{bpcov}=10\,r_{bpcov}$ | 功能清单逐项验证 |

> $S = S_{bpcov}$

**7.b.2 可设位置覆盖率**

> 度量目标：可正常设置断点的有效指令位置数 / 理论可调试位置总数；重点验证 Kernel 入口/出口、普通指令/分支/循环内部、普通函数/内联函数/模板展开代码、宏展开代码、编译器优化/指令重排后代码、Scalar/Vector/Cube/DMA 等不同指令、多核及不同任务中复用的相同代码地址
>
> $$ 可设位置覆盖率 = \frac{\text{能够正常设置断点的有效指令位置数}}{\text{理论可调试指令位置总数}} \times 100\% $$

$$ r_{loc} = \frac{\text{可设断点的有效指令位置数}}{\text{理论可调试位置总数}} $$
$$ S_{loc} = 10\,r_{loc} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{loc}$ | $S_{loc}=10\,r_{loc}$ | 指令地址空间逐点测试 |

> $S = S_{loc}$

**7.b.4 PC 定位准确率**

> 度量目标：实际暂停 PC 与期望 PC 一致的次数 / 断点命中总次数
>
> $$ PC 定位准确率 = \frac{\text{实际暂停 PC 与期望 PC 一致的次数}}{\text{断点命中总次数}} \times 100\% $$

$$ r_{pc} = \frac{\text{PC 一致的命中次数}}{\text{断点命中总次数}} $$
$$ S_{pc} = 10\,r_{pc} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{pc}$ | $S_{pc}=10\,r_{pc}$ | 断点命中后 PC 比对 |

> $S = S_{pc}$

**7.b.4 多核暂停一致率**

> 度量目标：所有目标 Core 在规定时间内进入稳定暂停的次数 / 暂停请求总次数
>
> $$ 多核暂停一致率 = \frac{\text{所有目标 Core 在规定时间内进入稳定暂停状态的次数}}{\text{暂停请求总次数}} \times 100\% $$

$$ r_{halt} = \frac{\text{所有目标核均稳定暂停的次数}}{\text{暂停请求总次数}} $$
$$ S_{halt} = 10\,r_{halt} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{halt}$ | $S_{halt}=10\,r_{halt}$ | 多核同步暂停测试 |

> $S = S_{halt}$

#### 可观测维度

**7.c.1 观测对象覆盖率**

> 度量目标：支持观测的硬件与软件对象数 / 调试调优要求观测的对象总数
>
> $$ 观测对象覆盖率 = \frac{\text{支持观测的硬件与软件对象数}}{\text{调试调优要求观测的对象总数}} \times 100\% $$

| 对象域 | 应支持的观测内容 |
| :-- | :-- |
| 计算 | Scalar、Vector、Cube 利用率、吞吐量和执行状态 |
| 搬运 | MTE/DMA 请求、带宽、时延、队列、拥塞和阻塞 |
| 存储 | Register、Local Memory、Cache、HBM 访问 |
| 同步 | Barrier、Event、跨核同步及等待关系 |

$$ r_{obscov} = \frac{\text{可观测对象数}}{\text{要求观测对象总数}} $$
$$ S_{obscov} = 10\,r_{obscov} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{obscov}$ | $S_{obscov}=10\,r_{obscov}$ | 逐对象验证+压力测试 |

> $S = S_{obscov}$

#### 性能度量

**7.d.1 事件类型覆盖率**

> 度量目标：可采集事件类型数 / 性能模型要求的事件类型总数；事件至少包括：指令发射/执行/完成/提交、流水忙/空闲/阻塞、Cache 访问/命中/未命中/驱逐、内存读写请求/返回/事务完成、DMA 开始/结束/超时、Barrier 进入/等待/退出、Kernel 开始/结束/排队/抢占
>
> $$ 事件类型覆盖率 = \frac{\text{可采集事件类型数}}{\text{性能模型要求的事件类型总数}} \times 100\% $$

$$ r_{evtcov} = \frac{\text{可采集事件类型数}}{\text{要求的事件类型总数}} $$
$$ S_{evtcov} = 10\,r_{evtcov} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{evtcov}$ | $S_{evtcov}=10\,r_{evtcov}$ | 功能清单逐项验证 |

> $S = S_{evtcov}$

**7.d.2 指标准确性**

> 度量目标：使用微基准、硬件仿真或已知工作量作为参考值，分别评价各维度的相对误差
>
> $$ 指标相对误差 = \frac{|\text{观测值} - \text{参考值}|}{\text{参考值}} \times 100\% $$

建议分别评价：指令计数误差、执行周期误差、Cache 命中率误差、内存流量和带宽误差、流水利用率误差、Stall 周期误差

$$ e_{rel} = \frac{1}{N}\sum_{i=1}^{N}\frac{|\text{测量值}_i - \text{基准值}_i|}{\text{基准值}_i} $$
$$ S_{acc} = 10\cdot\mathrm{clamp}\!\big(1-\frac{e_{rel}}{0.1},\,0,\,1\big) $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $e_{rel}$（$N$ 维度均值） | $S_{acc}=10\cdot\mathrm{clamp}\!\big(1-\frac{e_{rel}}{0.1},\,0,\,1\big)$ | 基准模型对照测量 |

> $S = S_{acc}$

**7.d.3 数据丢失率**

> 度量目标：因缓冲区溢出、带宽不足等丢失的记录数 / 理论应生成记录总数
>
> $$ 数据丢失率 = \frac{\text{因缓冲区溢出、带宽不足等丢失的记录数}}{\text{理论应生成记录总数}} \times 100\% $$

$$ r_{loss} = 1 - \frac{\text{丢失记录数}}{\text{理论应生成记录总数}} = \frac{\text{未丢失记录数}}{\text{理论应生成记录总数}} $$
$$ S_{loss} = 10\cdot\mathrm{clamp}\!\big(\frac{r_{loss} - 0.95}{0.05},\,0,\,1\big) $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{loss}$ | $S_{loss}=10\cdot\mathrm{clamp}\!\big(\frac{r_{loss} - 0.95}{0.05},\,0,\,1\big)$ | 高压流量测试+交叉验证 |

> $S = S_{loss}$

**7.d.4 瓶颈类型覆盖率**

> 度量目标：可归因的瓶颈类型覆盖率

| 瓶颈类型 | 关键观测指标 |
| :-- | :-- |
| 计算瓶颈 | 指令数、计算吞吐、流水利用率、依赖等待 |
| 内存带宽瓶颈 | 各层流量、峰值带宽、带宽利用率 |
| 内存时延瓶颈 | 请求时延、在途请求、依赖等待周期 |
| Cache 瓶颈 | 命中率、Miss 来源、驱逐和数据重用 |
| 搬运瓶颈 | DMA 带宽、队列深度、搬运等待 |
| 同步瓶颈 | Barrier 等待、Event 等待、核间不均衡 |
| 调度瓶颈 | 排队时延、资源不足和任务间空洞 |
| 并行度不足 | 活跃 Core、活跃 Block 和 Occupancy |
| 指令依赖瓶颈 | 数据依赖和 Scoreboard 等待 |

$$ r_{bncov} = \frac{\text{可归因的瓶颈类型数}}{\text{共 9 类}} $$
$$ S_{bncov} = 10\,r_{bncov} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{bncov}$ | $S_{bncov}=10\,r_{bncov}$ | 各瓶颈场景注入验证 |

> $S = S_{bncov}$

**7.d.5 Stall 原因解释覆盖率**

> 度量目标：能够归因到明确原因的阻塞周期 / 总阻塞周期（避免归入 Other/Unknown）；理想状态：总周期 ≈ 活跃周期 + 空闲周期 + ∑各类阻塞周期
>
> $$ Stall 原因解释覆盖率 = \frac{\text{能够归因到明确原因的阻塞周期}}{\text{总阻塞周期}} \times 100\% $$

$$ r_{expl} = \frac{\text{可归因到明确原因的 Stall 周期}}{\text{总 Stall 周期}} $$
$$ S_{expl} = 10\,r_{expl} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{expl}$ | $S_{expl}=10\,r_{expl}$ | 多场景性能数据采集 |

> $S = S_{expl}$

**7.d.6 低扰动与采集开销**

> 度量目标：开启观测的时间开销和行为扰动率
>
> $$ 时间开销 = \frac{T_{\text{开启观测}} - T_{\text{基线}}}{T_{\text{基线}}} \times 100\% $$
>
> $$ 行为扰动率 = \frac{\text{开启观测后行为或结果发生变化的用例数}}{\text{观测测试用例总数}} \times 100\% $$

重点验证：是否改变 Kernel 调度顺序、是否改变多流并发关系、是否改变 Cache 状态、是否改变竞争和死锁复现、是否引起异常降频、是否改变执行结果

$$ \delta_t = \frac{T_{\text{观测开启}}}{T_{\text{观测关闭}}} - 1,\quad \delta_b = \frac{\text{开启 vs 关闭行为差异}}{\text{关闭基线}} $$
$$ S_{time} = 10\cdot\mathrm{clamp}\!\big(1-\frac{\delta_t}{0.05},\,0,\,1\big),\quad S_{pert} = 10\cdot\mathrm{clamp}\!\big(1-\frac{\delta_b}{0.02},\,0,\,1\big) $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $\delta_t$、$\delta_b$ | $S = 0.55\,S_{time} + 0.45\,S_{pert}$ | A/B 对比计时 |

**7.d.7 端到端任务关联率**

> 度量目标：从软件对象关联到硬件事件的记录比例
>
> $$ 端到端关联率 = \frac{\text{能够从软件对象关联到硬件事件的记录数}}{\text{具备关联条件的硬件记录总数}} \times 100\% $$

$$ r_{tasklink} = \frac{\text{可自动关联到软件任务的硬件事件数}}{\text{硬件事件总数}} $$
$$ S_{tasklink} = 10\,r_{tasklink} $$

| 度量维度 | 可采集参数 | 评分公式 | 探测数据来源 |
| :-- | :-- | :-- | :-- |
| 硬件锚点 | $r_{tasklink}$ | $S_{tasklink}=10\,r_{tasklink}$ | 多级关联验证测试 |

> $S = S_{tasklink}$



### 3.8 Agent 友好

> 度量系统对 AI Agent / Copilot 自动生成算子代码的友好程度。以 Benchmark 算子的性能达成率度量：AI 自动生成的算子代码在目标硬件上的实测性能占该算子理论性能的比值，10 分为达成理论性能。

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 性能达成率 | $\eta_i=\frac{P_{agent,i}}{P_{theory,i}}$：对第 $i$ 个 Benchmark 算子，$P_{agent,i}$=AI Agent 自动生成的算子代码在目标硬件上的实测性能（TOPS/BW 等），$P_{theory,i}$=该算子的硬件理论峰值性能 | $S_i = 10\cdot\mathrm{clamp}\!\big(\eta_i,\,0,\,1\big)$ |

> **子项总分**：$S = \frac{1}{K}\sum_{i=1}^{K} S_i$，$K$ 为 Benchmark 算子个数
>
> **公式逻辑**：$\eta_i = 1$（Agent 生成代码达成理论性能）→ 满分 10 分；$\eta_i \le 0$ → 0 分；区间内线性映射。性能达成率越高，说明 Agent 越能生成接近最优的代码。
>
> **探测方式**：对每个 Benchmark 算子，使用 AI Agent/Copilot 自动生成算子代码，不经人工修改直接编译部署到目标硬件，运行相同 Benchmark 采集实测性能 $P_{agent,i}$；$P_{theory,i}$ 从硬件规格书获取该算子的理论峰值性能。

---

### 3.9 开发效率

> 度量开发多版本算子时的代码修改代价与性能提升比，衡量版本迭代的开发投入产出效率

#### 版本开发代价

**9.a.1 版本开发代价**

> 度量目标：同功能开发N个迭代版本（V1→V2→...→VN），度量每轮版本迭代的代码修改代价与性能提升比例，投入产出比越高则开发效率越高。

| 度量维度 | 可采集参数 | 评分公式 |
| :-- | :-- | :-- |
| 开发迭代版本数 | $N$＝同一功能（如 matmul/conv/reduce 等）实际开发的迭代版本数。$N=1$ 表示一版达成目标，无需迭代 | $S_N = 10\cdot\mathrm{clamp}\!\big(1-\frac{N-1}{5},\,0,\,1\big)$ |
| 相邻版本代码修改比 | $\gamma_i=\frac{\Delta L_i}{L_{i-1}}$：版本 $V_{i-1}$→$V_i$ 的代码修改行数 / 版本 $V_{i-1}$ 总代码行数，取各版本间均值 $\bar{\gamma}=\frac{1}{N-1}\sum_{i=2}^{N}\gamma_i$ | $S_{\gamma}=10\cdot\mathrm{clamp}\!\big(1-\frac{\bar{\gamma}}{0.3},\,0,\,1\big)$ |
| 相邻版本性能提升比 | $\rho_i=\frac{T_i}{T_{i-1}}$：版本 $V_i$ 执行时间 / 版本 $V_{i-1}$ 执行时间（$\rho_i\le 1$ 表示性能提升），取各版本间均值 $\bar{\rho}=\frac{1}{N-1}\sum_{i=2}^{N}\rho_i$ | $S_{\rho}=10\cdot\mathrm{clamp}\!\big(\frac{1-\bar{\rho}}{0.3},\,0,\,1\big)$ |

> **公式逻辑**：
> - 版本数 $N$：$N=1$（一次开发即达目标，无需迭代）→ 10 分；$N\ge 6$ → 0 分。版本数越少说明开发一次到位，迭代成本低
> - 代码修改比 $\bar{\gamma}$：$\bar{\gamma}=0$ → 10 分；$\bar{\gamma}\ge 30\%$ → 0 分。每轮迭代修改的代码越少，说明版本间改动轻量、架构稳定
> - 性能提升比 $\bar{\rho}$：$\bar{\rho}=1$（相邻版本性能无变化）→ 0 分；$\bar{\rho}\le 0.7$（每轮提升 30% 以上）→ 10 分。每轮性能提升越大，说明迭代收益越高
>
> **子项总分**：$S = 0.25\,S_N + 0.35\,S_{\gamma} + 0.40\,S_{\rho}$
>
> **探测方式**：
> - $N$：统计同一功能算子从初始版本到最终版本的实际开发迭代次数
> - $\gamma_i$：对每对相邻版本 $(V_{i-1}, V_i)$ 源码做 AST diff 分析，统计修改/新增/删除的行数 $\Delta L_i$，除以 $V_{i-1}$ 总代码行数 $L_{i-1}$，取 $N-1$ 轮迭代的均值
> - $\rho_i$：对每对相邻版本分别跑 Benchmark，记录执行时间 $T_{i-1}$ 和 $T_i$，计算比值 $\rho_i$，取 $N-1$ 轮迭代的均值

---
## 四、度量Pattern

> 来源：采集典型网络中，按照时间占比进行分析；要求每个Pattern保底权重 1/52; 大于1/52 按照时间权重分配0.5; 形成各Pattern 权重
> 
> - 度量权重： 易=1， 中=3，难=5
 

| # | 范式（算子） | 算子名 | 权重 | 系统规格 | 内存模型 | 计算模型 | 控制模型 | 访存模型 |
| :--: | :-- | :-- | :--: | :--: | :--: | :--: | :--: | :--: |
| 1 | Elementwise · silu/gelu/add | NA, 太简单 | 0.0627 | 易 | 易 | 易 | 易 | 易 |
| 2 | Broadcast · bias_add | NA, Norm 包含 | 0.0206 | 易 | 易 | 易 | 易 | 难 |
| 3 | Reduction · sum/mean/max | NA, Norm包含 | 0.0198 | 中 | 难 | 中 | 易 | 难 |
| 4 | Contraction · matmul/linear | Matmul，QuantMatmul | 0.1552 | 中 | 难 | 中 | 中 | 难 |
| 5 | ArgReduce · argmax/argmin | 补！ argmax | 0.0201 | 中 | 难 | 难 | 难 | 难 |
| 6 | Layout Transform · View | ViewCopy | 0.0395 | 难 | 中 | 中 | 易 | 难 |
| 7 | Padding · pad | 补！padding | 0.0192 | 难 | 难 | 难 | 难 | 难 |
| 8 | IndexGather · gather/embedding | GatherV2 | 0.0228 | 中 | 中 | 中 | 易 | 中 |
| 9 | ScatterUpdate · scatter | NA, ViewCopy 包含 | 0.0197 | 中 | 中 | 中 | 易 | 中 |
| 10 | AtomicUpdate · scatter_add | NA, ViewCopy 包含 | 0.0211 | 难 | 易 | 易 | 难 | 易 |
| 11 | Interpolation · grid_sample/roi_align | 补！ ResizeBilinearV2 | 0.0192 | 中 | 中 | 难 | 易 | 难 |
| 12 | MaskPredicate · where/masked_fill | NA, NPU 不太关注 | 0.0193 | 易 | 易 | 难 | 难 | 易 |
| 13 | SortSelect · topk/sort | NA, (QLI 覆盖) | 0.0197 | 难 | 难 | 难 | 难 | 难 |
| 14 | Histogram · bincount/histc | NA, NPU 不太关注 | 0.0192 | 中 | 中 | 中 | 易 | 中 |
| 15 | SlidingWindow · conv2d/pool | Conv2dV2 | 0.0193 | 难 | 难 | 难 | 难 | 难 |
| 16 | ControlFlow · cond/while_loop/hash | 补! ctcloss | 0.0196 | 易 | 易 | 易 | 中 | 易 |
| 17 | Recurrence · cumsum/lstm | 补! cumsum | 0.0192 | 中 | 难 | 难 | 难 | 难 |
| 18 | MegaKernel(片段) PTO尤其是控制流的度量 | MegaMoe | 0.0433 | 易 | 难 | 中 | 难 | 中 |
| 19 | Spectral · fft/dct | NA, NPU 不太关注 | 0.0192 | 难 | 难 | 难 | 难 | 难 |
| 20 | DynamicShape · 动态batch/序列；VariableOutput · nonzero/unique | NA, 非本次验证重点 | 0.0192 | 难 | 难 | 难 | 中 | 难 |
| 21 | RandomSampling · dropout/multinomial | NA, 非本次验证重点 | 0.0193 | 易 | 易 | 难 | 易 | 易 |
| 22 | Norm 类 | RmsNorm, GroupNormGrad | 0.0303 | 中 | 难 | 难 | 难 | 难 |
| 23 | Sparse · sparse.mm | NA, 非本次验证重点 | 0.0193 | 难 | 难 | 难 | 难 | 难 |
| 24 | Quantization · int8 GEMM/fp8 | DynamicMxQuant | 0.0402 | 易 | 易 | 难 | 中 | 中 |
| 25 | CollectiveCommunication · all_reduce/all_gather | dispatch+combine | 0.0518 | 中 | 易 | 易 | 难 | 中 |
| 26 | FusedComposite · softmax/flash_attention | QSMLA, QLI | 0.2211 | 难 | 难 | 难 | 难 | 难 |

### 4.1 各Pattern 易用性度量

> **目标**：计算单个 Benchmark 算子在所有度量子项上的综合易用性得分

对每个 Benchmark 算子 $j$（对应上表 26 个 Pattern），遍历所有度量子项 $c$（≈27 个活跃子项），逐项代入子项评分公式得到 $S_{c,j}$，按度量分类权重与子项权重加权聚合：

> **权重体系**：每个度量分类（x）权重 = 1（各分类等权）；分类内各度量子项（y.z）权重之和 = 1，见第 2 章各表格「子项权重」列。

$$
S_j = \frac{1}{N_{cat}} \sum_{cat} \sum_{c \in C_{cat}} w_c \cdot S_{c,j}
$$

其中 $N_{cat}$ 为活跃度量分类数（除去暂不度量的编译友好），$C_{cat}$ 为该度量分类下的度量子项集合，$w_c$ 为子项 $c$ 在分类内的权重（$\sum_{c \in C_{cat}} w_c = 1$）。

> **探测方式**：以算子 $j$ 的源码和 runtime profiling 数据为输入，逐项套用第三章节各子项评分公式（3.1~3.7），产出 25 维得分向量，再按公式聚合为单值 $S_j$。

---

### 4.2 系统规格与模型易用性度量

> **目标**：按度量分类维度（系统规格/内存模型/计算模型/控制模型/访存模型/调试调优）分别计算分类级易用性得分

对每个度量分类 $cat$，取 26 个 Benchmark 算子在该分类下各度量子项的加权均值：

$$
S_{cat} = \frac{\sum_{j=1}^{26} w_j \cdot S_{cat,j}}{\sum_{j=1}^{26} w_j}
$$

其中 $S_{cat,j}$ 为算子 $j$ 在该分类下所有度量子项的算术均值，$w_j$ 为上表各算子权重。

$$
S_{cat,j} = \frac{1}{|C_{cat}|} \sum_{c \in C_{cat}} S_{c,j}
$$

> **输出**：7 个分类级得分，反映系统在系统规格、内存模型、计算模型、控制模型、访存模型、调试调优、开发效率各维度的易用性强弱。

---

### 4.3 按照60%/90%/99% 易用性度量

> **目标**：在三档性能目标（60%/90%/99% 带宽利用率）下，计算综合易用性得分 $U$

三档分别独立评定。对每一档 $p \in \{60\%, 90\%, 99\%\}$：

$$
U^{(p)} = \sum_{cat} \omega_{cat} \cdot \frac{\sum_{j=1}^{26} w_j \cdot S_{cat,j}^{(p)}}{\sum_{j=1}^{26} w_j}
$$

其中 $S_{cat,j}^{(p)}$ 为算子 $j$ 在分档 $p$ 下、分类 $cat$ 的各子项得分均值（仅适用三档评定的子项取对应档得分，不区分档的子项三档共用同一得分）。

**三档独立输出**：$U^{(60\%)}$、$U^{(90\%)}$、$U^{(99\%)}$，不做平均，分别描述系统在基础可用（60%）、高效实用（90%）、极致打满（99%）三个层次的易用性表现。

**综合易用性**（若需单值）：

$$
U = \frac{1}{3} \big( U^{(60\%)} + U^{(90\%)} + U^{(99\%)} \big)
$$

---

### 4.4 Agent 友好

> **目标**：度量 26 个 Pattern 对应 Benchmark 算子在 AI Agent 自动生成代码后的性能达成率，10 分为达到该算子理论性能

对每个 Pattern $j$（$j=1,\dots,26$），计算该算子由 AI Agent 自动生成代码的实测性能与理论峰值性能的比值：

$$
\eta_j = \frac{P_{agent,j}}{P_{theory,j}}
$$

$$
S_{agent,j} = 10\cdot\mathrm{clamp}\!\big(\eta_j,\,0,\,1\big)
$$

其中 $P_{agent,j}$ 为 Agent 自动生成并部署的算子代码实测性能，$P_{theory,j}$ 为该算子在目标硬件上的理论峰值性能。

**Agent 友好综合得分**（26 个 Pattern 按权重加权）：

$$
S_{agent} = \frac{\sum_{j=1}^{26} w_j \cdot S_{agent,j}}{\sum_{j=1}^{26} w_j}
$$

其中 $w_j$ 为上表各算子权重。

> **公式逻辑**：$\eta_j = 1$（Agent 生成代码达成理论性能）→ 10 分；$\eta_j \le 0$ → 0 分；区间内线性映射。加权平均反映各 Pattern 对 Agent 友好的重要性差异。

**探测方式**：对每个 Pattern 对应的 Benchmark 算子，使用 AI Agent/Copilot 自动生成算子代码，不经人工修改直接编译部署到目标硬件，运行相同 Benchmark 采集实测性能 $P_{agent,j}$；$P_{theory,j}$ 从硬件规格书获取。
